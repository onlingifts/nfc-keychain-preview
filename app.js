const logoInput=document.getElementById('logoInput');const removeBg=document.getElementById('removeBg');const uploadEmpty=document.getElementById('uploadEmpty');const uploadFilled=document.getElementById('uploadFilled');const uploadThumb=document.getElementById('uploadThumb');const fileName=document.getElementById('fileName');const keychainEmpty=document.getElementById('keychainEmpty');const keychainWrap=document.getElementById('keychainWrap');const canvas=document.getElementById('keychainCanvas');const ctx=canvas.getContext('2d');const profileLogo=document.getElementById('profileLogo');const profilePlaceholder=document.getElementById('profilePlaceholder');let originalData='';
function readFile(file){if(!file||!file.type.startsWith('image/'))return;fileName.textContent=file.name;const r=new FileReader();r.onload=()=>{originalData=r.result;processLogo()};r.readAsDataURL(file)}
function processLogo(){if(!originalData)return;const img=new Image();img.onload=()=>{const max=1200,scale=Math.min(1,max/Math.max(img.width,img.height));const c=document.createElement('canvas');c.width=Math.max(1,Math.round(img.width*scale));c.height=Math.max(1,Math.round(img.height*scale));const x=c.getContext('2d',{willReadFrequently:true});x.drawImage(img,0,0,c.width,c.height);if(removeBg.checked){const p=x.getImageData(0,0,c.width,c.height),d=p.data;for(let i=0;i<d.length;i+=4){const r=d[i],g=d[i+1],b=d[i+2],mx=Math.max(r,g,b),mn=Math.min(r,g,b);if(mx>236&&mx-mn<24){d[i+3]=Math.min(d[i+3],Math.max(0,255-(mx-220)*16))}}x.putImageData(p,0,0)}const data=c.toDataURL('image/png');renderAll(data)};img.src=originalData}
function renderAll(data){uploadThumb.src=data;uploadEmpty.classList.add('hidden');uploadFilled.classList.remove('hidden');keychainEmpty.classList.add('hidden');keychainWrap.classList.remove('hidden');profileLogo.src=data;profileLogo.style.display='block';profilePlaceholder.style.display='none';drawKeychain(data)}
function drawKeychain(data){const img=new Image();img.onload=()=>{ctx.clearRect(0,0,canvas.width,canvas.height);const pad=58;const s=Math.min((canvas.width-pad*2)/img.width,(canvas.height-pad*2)/img.height);const w=img.width*s,h=img.height*s,x=(canvas.width-w)/2,y=(canvas.height-h)/2;ctx.save();ctx.shadowColor='rgba(0,0,0,.35)';ctx.shadowBlur=18;ctx.shadowOffsetY=18;ctx.drawImage(img,x,y,w,h);ctx.globalCompositeOperation='source-in';ctx.fillStyle='#c5a257';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.restore();ctx.save();ctx.globalAlpha=.18;ctx.translate(8,10);ctx.drawImage(img,x,y,w,h);ctx.globalCompositeOperation='source-in';ctx.fillStyle='#111';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.restore()};img.src=data}
logoInput.addEventListener('change',e=>readFile(e.target.files[0]));removeBg.addEventListener('change',processLogo);const dz=document.getElementById('dropZone');['dragenter','dragover'].forEach(ev=>dz.addEventListener(ev,e=>{e.preventDefault();dz.style.borderColor='#7159df'}));['dragleave','drop'].forEach(ev=>dz.addEventListener(ev,e=>{e.preventDefault();dz.style.borderColor=''}));dz.addEventListener('drop',e=>{const f=e.dataTransfer.files[0];if(f){readFile(f)}});
const bindings=[['company','companyPreview'],['whatsapp','waPreview'],['instagram','igPreview'],['website','webPreview']];bindings.forEach(([a,b])=>{const el=document.getElementById(a),out=document.getElementById(b);el.addEventListener('input',()=>out.textContent=el.value.trim()||el.placeholder)});
const orderBtn=document.getElementById('orderBtn');orderBtn.addEventListener('click',e=>{e.preventDefault();const company=document.getElementById('company').value.trim()||'بدون اسم';const msg=`مرحبا، جربت معاينة الميدالية والموقع لشركة ${company} وأريد الاستفسار عن الطلب.`;window.open(`https://wa.me/?text=${encodeURIComponent(msg)}`,'_blank')});
